import { assoc } from '.'

export const assignId = assoc('id')
export const assignClassName = assoc('className')
export const assignAs = assoc('As')
export const assignOnClick = assoc('onClick')
export const assignChildren = assoc('children')
